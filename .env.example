# Base URL for the Zara Challenge API
VITE_API_BASE_URL=

# API key sent as x-api-key header on every request
# Note: VITE_ prefix means this value is embedded in the client bundle.
# Acceptable here because this is a public challenge key, not a real secret.
VITE_API_KEY=
